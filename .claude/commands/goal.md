---
description: Give SUPERAGENT a goal to build or accomplish — lean, fast, effort matched to the work.
---
GOAL: $ARGUMENTS

Accomplish this goal NOW as SUPERAGENT, using the lean operating model:

1. **Scope it** — restate the goal in one line, pick the MINIMAL build that satisfies it, note what you will NOT build. Proceed autonomously.
2. **Build it — fan out as WIDE as possible** — decompose AGGRESSIVELY into as many independent pieces as the goal has (split by file, module, component, feature, or concern) and spawn one parallel subagent per piece, up to 16 per task. Default to MORE agents; only build in one thread for a genuinely atomic task.
3. **LEAN verification** — run the deliverable once, confirm it works (does what the goal asked, no errors), fix what's broken. Effort matched to the work — NO over-verification (no screenshots, no forced Grok, no endless loops). Go deeper only if the goal is genuinely risk-critical.
4. **Ship + log** — deliver the result concisely (box form), then append one `INTENT | SHIPPED | MATCH` line to LEARNINGS.md.

Get after it — fast.
